version: '3'

services:
  kafka-producer:
    build:
      context: ./
    environment:
      KAFKA_BROKER: kafka-broker:29092
    networks:
      - archimicroprojet_kafka_net

networks:
  archimicroprojet_kafka_net:
    external: true
