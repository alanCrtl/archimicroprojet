version: '3'
services:
  api:
    build:
      context: ../api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    networks:
      - kafkaproj
    restart: on-failure

networks:
  kafkaproj:
    driver: bridge
